import React from "react";
      export default function RiddleTest(props) {
        return (
          <div class="bg-danger rounded-2">new component created just now replacing RiddleTest</div>
        );
      }