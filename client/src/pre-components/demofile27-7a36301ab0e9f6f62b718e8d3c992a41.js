import React from "react";
      export default function NoodleTest(props) {
        return (
          <div class="bg-danger rounded-2">new component created just now replacing NoodleTest</div>
        );
      }